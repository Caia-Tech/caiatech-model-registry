# Copy to `.env` for local development (do not commit `.env`).
#
# Note: this project does not include dotenv loading by default.
# You can load variables in your shell with:
#   set -a; source .env; set +a

# Auth (required)
CAIA_REGISTRY_API_KEY=change-me
# CAIA_REGISTRY_API_KEYS=key1,key2
# CAIA_REGISTRY_TRUST_ACTOR_HEADER=0

# CORS (comma-separated). If unset, defaults to localhost-only.
# CAIA_REGISTRY_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# SQLite
# CAIA_REGISTRY_DB_PATH=./registry.db
CAIA_REGISTRY_SQLITE_BUSY_TIMEOUT_MS=5000

# Hashing limits (server will NOT hash files larger than this; clients can provide hashes/sizes)
CAIA_REGISTRY_MAX_HASH_BYTES=268435456

# Payload limits
CAIA_REGISTRY_MAX_METRICS_BODY_BYTES=65536
CAIA_REGISTRY_MAX_EVENT_PAYLOAD_BYTES=65536
CAIA_REGISTRY_MAX_WRITE_BODY_BYTES=262144

# Dev-only options (defaults are secure)
CAIA_REGISTRY_ALLOW_LOCAL_PATHS=0
CAIA_REGISTRY_EXPOSE_LOCAL_PATHS=0

# Optional promotion gating
# CAIA_REGISTRY_PROMOTION_REQUIRED_SUITE=core-v1
